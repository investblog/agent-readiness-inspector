---
name: i18n
description: Frontend internationalization conventions. Apply when adding or editing user-facing text in a multi-locale UI.
---

# i18n

The key namespaces are project-specific; this is the shared discipline.

## Two layers (do not overlap)
- **UI interface** (buttons, menus, forms, notifications, table headers) → locale files + `data-i18n` attribute binding.
- **Content pages** (About, Privacy, Terms, docs) → static text, NO `data-i18n`.

## Keys
- Dot notation, namespaced by context: `{section}.{category}.{key}` (e.g. `auth.errors.invalidEmail`, `layout.nav.home`).
- Not flat (`integrations`), not underscores (`auth_error`), not a camelCase full path (`cloudflareWizardTitle`).

## Binding — use the project's i18n mechanism
The framework's i18n layer binds keys to the UI (react-i18next / vue-i18n / Astro i18n / …) — use
what the project has, don't invent a parallel one. Only for a vanilla-JS UI with no framework, the
attribute-binding convention is: `data-i18n` (plain text), `data-i18n-html` (text with tags),
`data-i18n-aria`, `data-i18n-placeholder`, `data-i18n-title`.

## Discipline
[CRITICAL] A new key is added to **all** locales at once — never one locale only.

- New user-facing text → a key, never a hardcoded string.
